general:
* remove/disable 'save file' menu item for gcode files (high)
* ask about saving changes on opening another file (high)
* quick view buttons (low)
* axis labels (low)

stl:
* lay flat on platform (medium)

long-term:
* multi-threading for better responsiveness & speed (low)
* undo/redo (low)
